# create-ove-demo

Scaffold an interactive demo for the Open Visualisation Environment.

To run, clone this repository and run the following command:

```shell
pnpm run link
```

Once the package is available on your system, in the desired parent directory, run the following command and enter the required information:

```shell
create-ove-demo
```
